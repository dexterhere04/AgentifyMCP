# Origin of your always-on Agentify dashboard backend (hosted on a VM/VMaaS).
# The Vercel-hosted UI talks to this origin's /api. Unset for local dev (same-origin /api via the Vite proxy).
VITE_API_ORIGIN=https://your-vm.example.com
